<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when button is checked -->
    <item android:state_checked="true" android:drawable="@color/selectedColorRb" />
    <!-- State when button is pressed -->
    <item android:state_pressed="true" android:drawable="@color/pressedColor" />
    <!-- Default state -->
    <item android:drawable="@color/defaultColor" />
</selector>
