<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when the field is active (focused) -->
    <item android:color="@color/whiteEditText" android:state_focused="true" />
    <!-- Default state (inactive, not selected) -->
    <item android:color="@color/blackEditText" />
</selector>